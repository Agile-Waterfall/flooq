import { Dataflow, Edge, Node } from '../../source/Dataflow'
import { getEdges, getNodes, isLinearized } from '../testingUtils/GraphUtils'

describe( 'Executor', () => {
  it( 'must run', () => expect( true ).toBe( true ) )
} )
