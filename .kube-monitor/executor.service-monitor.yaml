apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kube-stack-prometheus-executor-monitor
  labels:
    k8s-app: kube-stack-prometheus-executor-monitor
spec:
  endpoints:
  - interval: 15s
    path: /metrics
    port: web
  selector:
    matchLabels:
      app: executor
  namespaceSelector:
    any: true
